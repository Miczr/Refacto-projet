import React from 'react';

function Header(){
    return(
        <header className="header">
            <p style={{fontSize: '30px', color: 'black', textAlign: 'center'}}>
            ! Welcom !
            </p>
        </header>
    )
}

export default Header;